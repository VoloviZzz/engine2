<!DOCTYPE html>
<html lang="ru">
    <head>
        <meta name="viewport" content="initial-scale=1, maximum-scale=1, user-scalable=no, width=device-width, height=device-height">
        <meta charset="utf-8">
        <meta content="yes" name="apple-mobile-web-app-capable" />
        <meta name="description" content="<%= route.seo_description %>">
        <meta name="keywords" content="<%= route.seo_keywords %>">
        <title><%= route.title -%> <%= ` - ` + siteConfig.get("appendTitle") -%></title>
		<link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Roboto+Slab:400,700&amp;subset=cyrillic">
        <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Roboto:100,300,400,500,600,700,900&amp;subset=cyrillic">
		<link rel="stylesheet" href="/vendors/font-awesome/css/font-awesome.min.css">
		<link rel="stylesheet" href="/css/main.min.css">
		<link rel="shortcut icon" href="<%= siteConfig.get("favicon") -%>">
		<% if(user.adminMode) { %>
			<link rel="stylesheet" href="/admins/css/admin.css">
		<% } %>
    </head>
    <body>
		<header class="document-header">
			<nav class="navbar navbar-expand-lg nav-letaindex bg-header-letaindex">
					<a class="navbar-brand">
						<img src="<%= Helpers.replaceImageIfNotExist(siteConfig.get("siteLogo")) -%>" <%= user.admin ? `id=toggle-admin-mode` : "" %>>
						<% if(user.adminMode) { %>
							<label class="js-siteLogo-edit-btn" style="cursor: pointer">
								<span>+</span>
								<input class="js-siteLogo-file" type="file" style="display: none;">
							</label>
						<% } %>
					</a>
					<button class="navbar-toggler ml-auto" type="button" data-toggle="collapse" data-target="#navbarMenu" aria-expanded="false" aria-label="Toggle navigation">
						<i class="fa fa-bars fa-lg py-1 text-white"></i>
					</button>
					<div class="collapse navbar-collapse justify-content-start"></div>
					<div class="collapse navbar-collapse justify-content-center" id="navbarMenu">
						<ul class="navbar-nav">
							<% HeaderRows.forEach((r) => { %>
								<li>
									<% if(user.adminMode) { %>
										<li class="nav-item mr-2 ml-2 dropdown" style="margin-top: 3px">
											<a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
												<span><%= r.title -%></span>
											</a>
											<div class="dropdown-menu" aria-labelledby="navbarDropdown">
												<input type="text" class="js-headerNav-edit dropdown-item" data-target="title" data-id="<%= r.id -%>" value="<%= r.title -%>">
												<input type="text" class="js-headerNav-edit dropdown-item" data-target="href" data-id="<%= r.id -%>" value="<%= r.href -%>">
												<div class="dropdown-divider"></div>
												<button class="btn js-headerNav-delete dropdown-item" style="display: block; width: 90%; margin: 0 auto" data-id="<%= r.id -%>">Удалить</button>
											</div>
										</li>
									<% } else { %>
										<li class="nav-item mr-3 ml-3">
											<a class="nav-link" href="<%= r.href -%>"><%= r.title -%></a>
										</li>
									<% } %>
								</li>
							<% }) %>
							<% if (user.id && user.admin) { %>
								<% if(user.adminMode) { %>
									<div>
										<button class="btn btn-admin-nav js-headerNav-add">+</button>
									</div>
								<% } %>
							<% } %>
						</ul>
					</div>
					<div class="collapse navbar-collapse justify-content-end" id="navbarMenu">
						<ul class="navbar-nav">
							<li class="nav-item">
								<a class="nav-link shopping-cart-link" href="/cart">
									<i class="fa fa-shopping-cart fa-lg"></i>
									<span 
										class="badge shopping-cart-badge" 
										id="js-shoppingCart-goodsCount" 
										style="<%= session.shoppingCart.totalCountGoods < 1 ? 'display: none' : '' -%>"><%= session.shoppingCart.totalCountGoods -%></span>
								</a>
							</li>
							<% if(!user.id) { %>
								<li class="nav-item">
									<a class="nav-link" href="/login?referer=<%= locals.route.reqUrl -%>">Вход</a>
								</li>
								<% } else { %>
									<% if (user.admin) { %>
										<li class="nav-item">
											<a class="nav-link" href="/admin" title="Перейти в админ. панель"><span class="fa fa-cogs"></span></a>
										</li>
									<% } %>
									<li class="nav-item">
										<a class="nav-link" href="/my"><%= user.clientName %></a>
									</li>
									<li class="nav-item">
										<a class="nav-link" href="/logout">Выйти</a>
									</li>
							<% }%>
						</ul>
					</div>
			</nav>
		</header>
		<!--<div class="aside-toggle">Меню слева</div>-->